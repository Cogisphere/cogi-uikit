/**
 *  Color definitions for colors used across the application. Note that these
 *  colors are defined in the CSS (the main styles.css file) and can be overriden
 *  by the host application via --cogi-uikit-color-* properties.
 */
export enum Color {
    dark = 'dark',
    red = 'red',
    blue = 'blue',
    green = 'green',
    orange = 'orange',
    yellow = 'yellow',
    purple = 'purple',
};